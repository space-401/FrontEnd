import styled from 'styled-components';

const MyPageWrapper = styled.div`
  width: 100%;
  max-width: 1856px;
  height: calc(100vh - 82px);
  padding-bottom: 16px;
  display: flex;
  gap: 7px;
`;

const S = {
  MyPageWrapper,
};

export default S;
