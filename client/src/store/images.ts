// import { create } from 'zustand';

// type ImagesType = {
//   images: string[];
//   setImages: React.Dispatch<React.SetStateAction<string[]>>;
// };

// export const useModalStore = create<ImagesType>((setState) => ({
//   images: [],
//   ModalOpen: () => setState(() => ({ isOpen: true })),
//   ModalClose: () => setState(() => ({ isOpen: false })),
// }));
